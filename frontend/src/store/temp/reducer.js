import * as types from "./actionTypes";

const initData = {};

export const authReducer = (state = initData, { type, payload }) => {
  switch (type) {
    case "value":
      break;

    default:
      return state;
  }
};
